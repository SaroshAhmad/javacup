/**
 * Login (/login) — placeholder.
 * Real auth (Supabase email + Google OAuth) is wired here in a later sprint.
 */
export default function Login() {
  return (
    <div className="mx-auto max-w-form px-4 py-24 text-center">
      <div className="mb-3 font-mono text-mono-label text-text-muted">// join</div>
      <h1 className="font-display text-heading-1 text-text-primary">Join JavaCup</h1>
      <p className="mt-3 text-body-lg text-text-secondary">
        Sign up free. No credit card. No spam. Just a community that has your back.
      </p>
    </div>
  );
}
